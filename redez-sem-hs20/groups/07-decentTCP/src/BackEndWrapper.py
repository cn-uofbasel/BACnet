import SocketWrapper

class BackEndWrapper:

    def __init__(self, lulw, omeganay, hidin):
        self.SocketWrapper(lulw, omeganay, hidin)
    
    def send(sendStr) -> None:
        self.SocketWrapper.send(sendStr)

